# Default values for mapgis.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: xxx
fullnameOverride: mapgis-xxx

appPort: 32000

namespace: mapgis-xxx

imageRegistry: 
imageNamespace: mapgis
imageNfsClientProvisioner: sig-storage/nfs-subdir-external-provisioner:v4.0.2
imageRepository: xxx-linux
imageTag: "10.6.4.10"
imagePullPolicy: IfNotPresent
imagePullSecret: 

nfsServer: 
nfsPath: /
nfsStorageClassName: 

resourceCpuLimit: 2
resourceMemoryLimit: 4Gi